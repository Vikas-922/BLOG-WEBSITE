// Define different size limits for images and videos
const MAX_IMAGE_SIZE = (1500 + 24) * 1024; // 1.5 MB for images in bytes
const MAX_VIDEO_SIZE = (15000 + 24) * 1024; // 15 MB for videos
const MAX_AUDIO_SIZE = (3000 + 24) * 1024; // 3 MB for videos

export { MAX_IMAGE_SIZE, MAX_VIDEO_SIZE, MAX_AUDIO_SIZE };